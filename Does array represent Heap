 bool isMaxHeap(int a[], int n)
    {
        // Your code goes here
        int i,j;
        
        for(i=n-1;i>0;i--)
        {
            j=(i-1)/2;
            
            if(a[j]<a[i])
            return false;
        }
        
        return true;
    }
