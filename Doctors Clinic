 int waitingTime(int N, int X){
        // code here
        int last=(N-1)*X;
        int wait=(N-1)*10;
        
        if(wait>last)
        return wait-last;
        else 
        return 0;
    }
