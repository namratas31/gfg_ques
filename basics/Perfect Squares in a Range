int numOfPerfectSquares(int a, int b) {
        // code here
        int count;
        int k=pow(a,0.5);
        int m=pow(b,0.5);
        
        if(k*k==a)
        count=m-k+1;
        
        else
        count=m-k;
            
        return count;
        
    }
