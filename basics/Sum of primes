int primeSum(int N){
        // code here
        int k,i=N,sum=0;
        while(i!=0)
        {
            i=N/10;
            k=N%10;
            N=i;
            if(k==2 || k==3 || k==5 || k==7)
            {
                sum=sum+k;
            }
        }
        return sum;
    }
