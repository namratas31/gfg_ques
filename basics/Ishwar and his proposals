vector <int> acceptedProposals (vector <int> &arr, int n)
    {
        //code here.
        sort(arr.begin(),arr.end());
        
        vector<int> p;
        
        p.push_back(arr[n-2]);
        p.push_back(arr[1]);
       
        return p;
    }
