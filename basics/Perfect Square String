int isSquare(string S){
    //complete the function here
    int i,sum=0,x;
    
    for(i=0;i<S.length();i++)
    {
        sum+=S[i];
    }
    
    x=pow(sum,0.5);
    
    if(x*x==sum)
    return 1;
    else 
    return 0;
    
}
