int EvenOdd(string n1 , string n2)
{
    //code here.
    int j,k,m,n,val;
   j=n1.length();
   k=n2.length();
   m=(int)n1[j-1]-48;
   n=(int)n2[k-1]-48;
   val=m*n;
   
   if(val%2==0)
   {
       return 1;
   }
   else
   {
       return 0;
   }
