int minValueToBalance(int a[], int n)
    {
       //code here.
       int i,sum1=0,sum2=0;
       
       for(i=0;i<n/2;i++)
       {
           sum1+=a[i];
       }
       
       for(i=n/2;i<n;i++)
       {
           sum2+=a[i];
       }
       
           return abs(sum2-sum1);
       
    }
