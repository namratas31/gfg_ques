 string PartyType(long long int a[], int n)
    {
        // Your code goes here
        sort(a,a+n);
        int i;
        
        for(i=0;i<n-1;i++)
        {
            if(a[i+1]==a[i])
            return "BOYS";
        }
        
        return "GIRLS";
    }
