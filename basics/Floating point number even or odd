bool isEven(string s,int n)
{
    // code here.
    int i;
    
    for(i=n-1;i>=0;i--)
    {
        if(s[i]!='0' && s[i]!='.')
        {
            if((s[i]-48)%2==0)
            return 1;
            else 
            return 0;
        }
    }
    
    return 1;
}
