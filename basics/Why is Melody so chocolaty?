
int max_adjacent_sum(int arr[], int n)
{
    // Complete the function
    int i,max=INT_MIN,sum;
    
    for(i=0;i<n-1;i++)
    {
        sum=arr[i+1]+arr[i];
        if(sum>max)
        max=sum;
    }
    
    return max;
}
