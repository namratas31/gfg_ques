string snakeCase(string S , int n) {
        // code here
        transform(S.begin(), S.end(), S.begin(), ::tolower);
        
        for(int i=0;i<n;i++)
        {
            if(S[i]==' ')
            {
                S[i]='_';
            }
        }
       
        return S;
    }
};
