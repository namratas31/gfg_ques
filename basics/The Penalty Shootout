	int penaltyScore(string S)
	{
	    // Your code goes here
	    int i=0,count=0;
	    while(S[i]!='\0')
	    {
	        if(S[i]=='2')
	        {
	            if(S[i+1]=='1')
	            {
	                count++;
	            }
	        }
	        i++;
	    }
	     
	     return count;
	}
