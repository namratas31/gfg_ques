long long findDiff(long long amount){
    //code
    string amt=to_string(amount);
    long long i=0,num;
    
    while(amt[i]!='\0')
    {
        if(amt[i]=='6')
        {
            amt[i]='9';
        }
        i++;
    }
    
    num=stoi(amt);
    
    return num-amount;
    
