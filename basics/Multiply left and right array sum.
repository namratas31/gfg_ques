int multiply(int arr[], int n)
{
    // Complete the function
    int max1=0,max2=0;
    for(int i=0;i<n/2;i++)
    {
        max1+=arr[i];
    }
     for(int i=n/2;i<n;i++)
    {
        max2+=arr[i];
    }
    return max1*max2;
}
