vector<int> kLargest(int arr[], int n, int k)
    {
        // code here
        vector<int>max;
        int i;
        sort(arr,arr+n);
        
        for(i=n-1;i>=n-k;i--)
        {
            max.push_back(arr[i]);
        }
        
        return max;
    }
