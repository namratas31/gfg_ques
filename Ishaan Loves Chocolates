int chocolates(int arr[], int n)
{
    // Complete the function
    int i=0,k=n-1;
    while(i!=k)
    {
        if(arr[i]>=arr[k])
        {
            i++;
        }
        else 
        {
            k--;
        }
    }
    return arr[i];
}
