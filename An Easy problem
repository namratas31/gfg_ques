int easyProblem(int K, int L, int R, int X, int Y) {
        // code here
        int i;
        for(i=X;i<=Y;i++)
        {
            if((i*K)>=L && (i*K)<=R)
            return 1;
        }
        
        return 0;
    }
