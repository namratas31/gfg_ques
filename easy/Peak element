int peakElement(int arr[], int n)
{
   // Your code here
   int max=arr[0],count;
   
   for(int i=0;i<n;i++)
   {
       if(arr[i]>=max)
       {
           max=arr[i];
           count=i;
       }
   }
   return count;
   
}
