string removeDuplicates(string str) {
	    // code here
	    unordered_map<char,int>mp;
	    int i;
	    string ans="";
	    
	    for(i=0;i<str.length();i++)
	    {
	       mp[str[i]]++;
	       
	       if(mp[str[i]]==1)
	       ans+=str[i];
	    }
	    
	    return ans;
	}
