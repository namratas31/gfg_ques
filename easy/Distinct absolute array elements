int distinctCount(int arr[], int n) {
        // code here
        set<int>s;
        int i;
        
        for(i=0;i<n;i++)
        {
            s.insert(abs(arr[i]));
        }
        
        return s.size();
    }
