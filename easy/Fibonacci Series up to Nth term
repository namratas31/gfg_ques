 vector<long long> Series(int N) {
        // COde here
        long long a=0,b=1,temp;
        vector<long long>ans;
        
        for(int i=0;i<=N;i++)
        {
            ans.push_back(a);
            temp=a+b;
            a=b;
            b=temp;
            
        }
        
        return ans;
    }
