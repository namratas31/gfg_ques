int corner_digitSum(int n){
   	    // Code here
   	    int sum=0,len;
   	    string s;
   	    s=to_string(n);
   	    len=s.length();
   	    
   	    if(s.length()==1)
   	    return n;
   	    
   	    sum+=n%10;
   	    sum+=n/pow(10,len-1);
   	    
   	    return sum;
   	}    
