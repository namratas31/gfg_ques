 bool possible(long long arr[], long long n)
    {
        //code here.
        long long i,sum=0;
        
        for(i=0;i<n;i++)
        {
           sum+=arr[i]; 
        }
        
        if(sum==(n*(n+1))/2)
        return 1;
        else 
        return 0;
    }
