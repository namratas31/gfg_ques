long long int squaresDiff(int N){
        // code here
        
        long long int sum1,sum2;
        
        sum1=N*(N+1)*(2*N+1);
        sum1/=6.0;
        
        sum2=(N*(N+1))/2.0;
        sum2*=sum2;
        
        return abs(sum1-sum2);
        
    }
