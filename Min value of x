int minX(int a, int b, int c, long int k){
        //complete the function here
        long int i=0,sum;
        sum=c;
        
        while(sum<k)
        {
            sum=a*i*i+b*i+c;
            if(sum<k)
            i++;
        }
        
        return i;
        
    }
