int digitPrime(int N){
	    //code here
	    int i;
	    
	    N=N%9;
	    if(N==0)
	    N=9;
	    
	    if(N<=1)
	    return 0;
	    
	    for(i=2;i<=sqrt(N);i++)
	    {
	        if(N%i==0)
	        return 0;
	    }
	    
	    return 1;
	}
