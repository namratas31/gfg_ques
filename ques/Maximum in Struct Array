int findMax(struct Height arr[], int n)
{
    // Code here
    int max=arr[0].feet,len;
    for(int i=0;i<n;i++)
    {
        len=arr[i].feet*12+arr[i].inches;
        if(len>max)
        max=len;
    }
    return max;
    
}
