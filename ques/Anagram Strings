int areAnagram(string S1, string S2) {
        // code here
        sort(S1.begin(),S1.end());
        sort(S2.begin(),S2.end());
        
        if(S1==S2)
        return 1;
        else 
        return 0;
    }
