int isPrime(int N){
        // code here
        int i,k;
        
        k=pow(N,0.5);
        
        if(N==1)
        return 0;
        
        for(i=2;i<=k;i++)
        {
            if(N%i==0)
            return 0;
        }
        
        return 1;
    }
