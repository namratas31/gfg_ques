string isGoodString(string s)
{
    //code here.
    int i;
    
    for(i=0;i<s.length()-1;i++)
    {
        if(abs(s[i+1]-s[i])==1 || abs(s[i+1]-s[i])==25)
        continue;
        else
        return "NO";
    }
    
    return "YES";
}
