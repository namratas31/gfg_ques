 int maxDays(int arr[],int n){
        // code here  
        int max=arr[0],i;
        for(i=0;i<n;i++)
        {
            if(arr[i]>max)
            max=arr[i];
        }
        return max;
    }
