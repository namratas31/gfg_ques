long long int countDigits(long long int a, long long int b) { 
        // code here
        long long int prod=a*b,count=0;
        while(prod!=0)
        {
            prod=prod/10;
            count++;
        }
        return count;
    }
