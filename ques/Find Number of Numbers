int num(int a[], int n, int k)
{
     //add code here.
     int i,count=0;
     
     for(i=0;i<n;i++)
     {
         while(a[i]>0)
         {
             if(a[i]%10==k)
             count++;
             a[i]=a[i]/10;
         }
     }
     
     return count;
}
