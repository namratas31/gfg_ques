long long jumpyBall(long long N)
    {
        // Write Your Code here
        long long count=0,k=N;
        
        while(k>0)
        {
            count+=2*k;
            k=k/2;
        }
        
        return count;
    }
