int getCount(struct Node* head)
     {
        struct Node* temp;
        temp=head;
        int count=0;
        while(temp!=0)
        {
            count++;
            temp=temp->next;
        }
        return count;
     }
